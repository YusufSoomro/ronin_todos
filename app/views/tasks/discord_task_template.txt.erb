Most recent unfinished task
- [<%= Task::CHECK_MARK if @first_unfinished_task.is_really_done? %>] <%= @first_unfinished_task.description %>
  <% if @first_unfinished_task.subtasks.count > 0 %>
  <% @first_unfinished_task.subtasks.each do |subtask| %>
    <%= Task::BULLET_POINT %> [<%= Task::CHECK_MARK if subtask.is_done %>] <%= subtask.description %>
  <% end %>
  <% end %>